---
title: Blog
layout: default
---

<h1 class="text-5xl font-sans">Blog</h1>
<div class="flex flex-col mt-5">
    {% for year, yearPosts in collections.postsByYear %}
        <div class="flex flex-row p-3">
            <h3 class="text-bold text-3xl">{{ year }}</h3>
            <ul class="list-outside ml-8 mb-2">
                {% for post in yearPosts %}
                    <li class="mb-4">
                        <p class="m-0">
                            <div class="py-2 font-sans font-bold text-xl">
                                <a class="" href="{{ post.url }}">
                                    {{ post.data.title }}
                                </a>
                            </div>
                            {% for tag in post.data.tags %}
                                {% if tag != 'posts' %}
                                    {% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
                                    <a class="no-underline" href="{{ tagUrl }}">
                                        <span class="inline-block px-2 leading-loose bg-sky-700 text-white font-sans rounded text-xs">{{ tag }}</span>
                                    </a>
                                {% endif %}
                            {% endfor %}
                        </p>
                        <p class="mt-2 text-gray-600">{% excerpt post %}</p>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endfor %}
</div>